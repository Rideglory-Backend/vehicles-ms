{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Vehicles MS",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run start -- --debug 9230 --watch",
      "cwd": "${workspaceFolder}"
    }
  ]
}