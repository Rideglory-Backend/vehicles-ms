---
name: my-custom-agent
description: "One-line picker description."

Examples:

- user: "Example request"
  assistant: "Example reply."
  (Launch the Agent tool with the my-custom-agent agent)

model: sonnet
color: blue
skills: []
---

# Custom agent

Define behavior below. If this maps to a framework role, read **`CLAUDE.md`** and **`workflow/state.json`**, and align outputs with the closest role’s handoff format.

Optional: **`$ARGUMENTS`** from the caller.
