# Iteration history

Append-only log. Each **`/iter N`** close-out (PO phase `po_close`) adds one row. Do not delete prior rows.

| Iteration | Closed (UTC) | Summary | Details |
|-----------|--------------|---------|---------|
| — | — | — | Add rows starting after the first completed `/iter` |
