# Iteration context (bridge to next)

**Maintainer:** Product Owner (PO), at the end of each `/iter N` (Phase 9 — close-out).

**Purpose:** Give the *next* planning or execution run a short, explicit handoff: what shipped, what was deferred, risks, and what to prioritize in iteration **N+1**.

---

## Latest

- *Not set — completed iteration close-out will overwrite this section.*
